{
  "name": "diff-limitation-checker",
  "version": "1.0.0",
  "description": "PR should not too many files, if not it need to be explained why",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ahiho/diff-limitation-checker.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ahiho/diff-limitation-checker/issues"
  },
  "homepage": "https://github.com/ahiho/diff-limitation-checker#readme",
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.37.0",
    "jest": "^29.5.0"
  }
}
